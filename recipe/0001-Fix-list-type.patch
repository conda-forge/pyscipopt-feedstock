From 4651eb3983fcad0b8e6b1466bc7e03b5a2f43161 Mon Sep 17 00:00:00 2001
From: AntoinePrv <AntoinePrv@users.noreply.github.com>
Date: Wed, 4 Feb 2026 14:27:03 +0100
Subject: [PATCH] Fix list type

---
 setup.py | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/setup.py b/setup.py
index 9605ef1..369f796 100644
--- a/setup.py
+++ b/setup.py
@@ -13,7 +13,7 @@ if not scipoptdir:
     conda_prefix = os.environ.get("CONDA_PREFIX", "").strip('"')
 
     if conda_prefix and os.path.exists(os.path.join(conda_prefix, "include")):
-        includedirs = os.path.join(conda_prefix, "include")
+        includedirs = [os.path.join(conda_prefix, "include")]
         libdir = os.path.join(conda_prefix, "lib")
         libname = "libscip" if platform.system() == "Windows" else "scip"
         print(f"Detected conda environment at {conda_prefix}.")
-- 
2.52.0

